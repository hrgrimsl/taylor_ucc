name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
    - pyscf
    - python-git
    - opt_einsum

    #Package dependencies
  - numpy
  - scipy


    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

