name: test
channels:

  - conda-forge
  - pyscf
  - defaults

dependencies:
    # Base depends
  - python
  - pip
  

    #Package dependencies
  - numpy
  - scipy
  - pyscf

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

