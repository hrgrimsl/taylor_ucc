name: test
channels:

  - conda-forge
  - defaults

dependencies:
    # Base depends
  - python
  - pip:
    - pyscf  
    - opt_einsum
    - python-git
    - psutil

    #Package dependencies
  - numpy
  - scipy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

